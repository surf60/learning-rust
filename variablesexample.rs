fn main(){
    let age = 31;
    let name = "surf";
    // printting a variable
    print!("{}",age);
    // "{}" is a placeholder
    println!("{}, age = {}", name, age);
    // each variable replaces placeholder x in sucsession
}